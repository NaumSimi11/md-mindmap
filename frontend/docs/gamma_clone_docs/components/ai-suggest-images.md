# AI Suggest Images — Component & Service

## Purpose
UI component that surfaces image suggestions for a block.

## Flow
1. UI sends request with block text + theme palette hints
2. Service returns candidate images with scores
3. UI displays thumbnails, allow quick apply

## UX
- Show license and source
- Show "palette match" indicator
- Allow replace/all blocks
