# Drag Layout Onto Block — UX Spec

## Purpose
Drag a layout template and drop onto an existing block to morph it.

## Runtime
- Precompute mapping heuristics for common conversions
- Offer animated morph preview
- Use optimistic update with undo token
