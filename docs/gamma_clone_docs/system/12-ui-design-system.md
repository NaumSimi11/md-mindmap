# UI Design System — Tokens & Patterns

## Tokens
- Colors, spacing, radius, typography tokens as JSON
- Expose tokens via CSS variables

## Patterns
- Block inspector (right panel) for per-block settings
- Floating block toolbar for quick actions
- Contextual AI button per block
- Theme switcher (global)

## Motion
- Use reduced-motion user preference
- Animate layout transitions with FLIP technique
